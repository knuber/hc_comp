SELECT
	DKFSYY||DIGITS(DKFSPR) AS PERD,
	DIGITS(DKACC#) AS DKACC#,
	DKADDD,
	SUM(DKAMT) AS AMT
FROM
	LGDAT.GLSBAR
WHERE
	DKFSYY = 13 AND
	DKFSPR <= 6 AND
	DKSRCE = 'AR' AND
	DKQUAL = 'RC' AND
	SUBSTR(DKACC#,7,4) = '5030'
GROUP BY
	DKFSYY||DIGITS(DKFSPR),
	DIGITS(DKACC#),
	DKADDD
