SELECT
	FHCOM#, FHBANK, FHVEN#, BTNAME, DIGITS(FHPYY)||DIGITS(FHPPER) AS PERIOD,  SUM(FHCBAL) AS AMOUNT
FROM
	LGDAT.OPEN
	LEFT OUTER JOIN LGDAT.VEND ON
		BTVEND = FHVEN#
WHERE
	FHPYY <= 13 AND
	FHCBAL <> 0
GROUP BY
	FHCOM#, FHBANK, FHVEN#, BTNAME, DIGITS(FHPYY)||DIGITS(FHPPER)