SELECT
	KBPT#, KBGL#, KBUPRC, KBPLNT, KBQTYO, KBQTYR, KAODAT,
	KAOVND, KAOVNM, KASVND, KASVNM, KAPO#, KASCRY
FROM
	LGGL.POI 
	LEFT OUTER JOIN LGDAT.POH POH ON
		KAPO# = KBPO#
WHERE 
	KBPT# = 'QADCARD.BOX1'