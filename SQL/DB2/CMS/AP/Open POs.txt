SELECT
	KBPO#, CHAR(KBRDAT) KBODAT, KBISTS, XLSTAT, SUM(KBEXTN)
FROM
	LGDAT.POI
	LEFT OUTER JOIN LGDAT.PARQ ON
		XLPO# = KBPO#
WHERE
	KBISTS <> 'C' AND
	XLSTAT = 'C'
GROUP BY
	KBPO#, CHAR(KBRDAT), KBISTS, XLSTAT
HAVING
	SUM(KBEXTN) >= 100000
FETCH FIRST 100 ROWS ONLY