SELECT
	DISTINCT UIBTID
FROM	
	LGDAT.RPRM
	INNER JOIN LGDAT.RPRH ON
		NWBTID = UIBTID
	EXCEPTION JOIN QGPL.FFPDGLR1 ON
		BTID = UIBTID AND
		ENT# = UIENT# AND
		ACTN = 'BMT' AND
		SRCE = 'RPRM'
WHERE
	NWFSYY = 14 AND
	NWFSPP = 7 AND
	UITQTY <> 0