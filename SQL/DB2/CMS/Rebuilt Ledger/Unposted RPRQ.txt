SELECT
	TIBTID, TIENT#, TIJOB#, TIPART, TIQTYP
FROM	
	LGDAT.RPRQ
	EXCEPTION JOIN QGPL.FFPDGLR1 ON
		BTID = TIBTID AND
		ENT# = TIENT# AND
		ACTN = 'CPG' AND
		SRCE = 'RPRQ'
WHERE
	TICDAT >= '2014-07-01' AND
	TIQTYP <> 0
FETCH FIRST 100 ROWS ONLY