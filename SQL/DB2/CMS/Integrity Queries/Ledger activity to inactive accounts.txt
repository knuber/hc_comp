SELECT 
	DKSRCE, DKQUAL, DKACC#, DKAMT, DKFSYY, DKFSPR
FROM
	LGDAT.GTLYN
	LEFT OUTER JOIN LGDAT.MAST ON
		AZCOMP||DIGITS(AZGL#1)||DIGITS(AZGL#2) = DKACC#
WHERE
	AZSTAT = 'I' AND
	DKFSYY = 13 AND
	DKFSPR = 10
FETCH FIRST 10 ROWS ONLY