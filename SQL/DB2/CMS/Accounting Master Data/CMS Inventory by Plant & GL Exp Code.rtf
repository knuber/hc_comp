SELECT 
	 BXPLNT, COALESCE(AVGLED, AWGLED) AS GLED, SUM(BXQTOH)
FROM 
	LGDAT.STKB 
	LEFT OUTER JOIN LGDAT.STKMM ON
		AVPART = BXPART
	LEFT OUTER JOIN LGDAT.STKMP ON
		AWPART = BXPART
WHERE 
	BXPART <> ''
GROUP BY
	BXPLNT, COALESCE(AVGLED, AWGLED)
ORDER BY BXPLNT
