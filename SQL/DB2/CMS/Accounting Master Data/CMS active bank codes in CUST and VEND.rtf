SELECT
	*
FROM
(
SELECT
	DISTINCT BTBANK AS BANK
FROM 
	LGDAT.VEND
WHERE
	BTSTAT = 'A' AND
	BTBANK <> ''


UNION

SELECT
	DISTINCT BVARCD AS BANK
FROM
	LGDAT.CUST
WHERE
	BVSTAT = 'A'
) X 
ORDER BY 
	BANK
