SELECT
	AZCOMP||DIGITS(AZGL#1)||DIGITS(AZGL#2) AS ACCT, AZFUT2, AZTITL, AZSTAT, AZGROP, '',HYPOLD, HYFUT
FROM
	LGDAT.MAST
	LEFT OUTER JOIN LGPGM.CMSTOHYP ON
		CMSGL = AZCOMP||DIGITS(AZGL#1)||DIGITS(AZGL#2)
WHERE
	AZCOMP||DIGITS(AZGL#1)||DIGITS(AZGL#2) IN (570000200707)