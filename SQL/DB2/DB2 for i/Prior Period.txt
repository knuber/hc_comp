CREATE VIEW FANALYSIS.VW_PRFSPR AS
SELECT
	B.COMP, B.FSPR CURRP, O.FSPR PRIORP
FROM
	FANALYSIS.VW_FFGLPD B
	LEFT OUTER JOIN FANALYSIS.VW_FFGLPD O ON
		DAYS(O.EDAT) = DAYS(B.SDAT) -1 AND
		O.COMP = B.COMP